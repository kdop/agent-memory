# Live database — binary, changes constantly, not version-controlled.
# Back it up out-of-band; do not commit.
memory.db
memory.db-journal
memory.db-wal
memory.db-shm

# Python
.venv/
__pycache__/
*.pyc

# Build artifacts (from `pip install` / packaging)
build/
*.egg-info/

# Editor / IDE
.idea/
.vscode/
